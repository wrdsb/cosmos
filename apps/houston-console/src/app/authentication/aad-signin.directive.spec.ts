import { AadSigninDirective } from './aad-signin.directive';

describe('AadSigninDirective', () => {
  it('should create an instance', () => {
    const directive = new AadSigninDirective();
    expect(directive).toBeTruthy();
  });
});
