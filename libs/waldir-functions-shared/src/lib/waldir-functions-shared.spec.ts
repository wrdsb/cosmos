import { waldirFunctionsShared } from './waldir-functions-shared';

describe('waldirFunctionsShared', () => {
  it('should work', () => {
    expect(waldirFunctionsShared()).toEqual('waldir-functions-shared');
  });
});
