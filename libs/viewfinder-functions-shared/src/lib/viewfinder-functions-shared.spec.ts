import { viewfinderFunctionsShared } from './viewfinder-functions-shared';

describe('viewfinderFunctionsShared', () => {
  it('should work', () => {
    expect(viewfinderFunctionsShared()).toEqual('viewfinder-functions-shared');
  });
});
